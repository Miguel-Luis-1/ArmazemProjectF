name: armazemf
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_mobx: ^2.0.6+1
  mobx: ^2.0.7+2
  flutter_modular: ^6.3.3

dev_dependencies:
  build_runner: ^2.1.11
  mobx_codegen: ^2.0.6+3
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
